import {useEffect,useState} from 'react';
import {Header,Footer} from '../../components/site-chrome';
const status:Record<string,string>={available:'Kaynak verisi',cached:'Son kayıt · son güncelleme başarısız',stale:'Eski dönem · güncel kabul etmeyin',not_configured:'Erişim / seri yapılandırılmamış',unavailable:'Veri yok',error:'Kaynağa erişilemedi'};
export default function Data(){const[data,set]=useState<any[]>([]),[error,fail]=useState(''),[loading,load]=useState(true);async function refresh(){load(true);fail('');try{const r=await fetch('/api/market-data',{signal:AbortSignal.timeout(12000)});if(!r.ok)throw Error('Veri servisine erişilemiyor. Aşağıdaki resmî kaynakları kullanabilirsiniz.');const j=await r.json();if(!Array.isArray(j.data))throw Error('Veri yanıtı doğrulanamadı.');set(j.data)}catch(e){fail(e instanceof Error?e.message:'Veri alınamadı.')}finally{load(false)}}useEffect(()=>{refresh()},[]);return <><Header/><main className="inner"><div className="page-title"><p className="eyebrow">VERİ ŞEFFAFLIĞI</p><h1>Kaynak, dönem ve bağlantı durumu</h1><p>Geçmiş veriler gelecekteki getirinin tahmini değildir. Hesaplayıcıdaki örnek oranlar otomatik olarak resmî veri sayılmaz.</p></div><button onClick={refresh} disabled={loading}>{loading?'Kontrol ediliyor…':'Bağlantıyı tekrar kontrol et'}</button>{error&&<p className="error" role="alert">{error}</p>}<div className="source-list">{data.map(d=><article key={d.series}><div><h2>{d.label}</h2><p><b>{d.value==null?'Veri yok':Number(d.value).toLocaleString('tr-TR',{maximumFractionDigits:4})+' '+d.unit}</b> · Dönem: {d.period||'—'}</p>{d.annualChange!=null&&<p>Aynı ayın yıllık değişimi: %{d.annualChange.toFixed(2)}</p>}<span className="source-status">{status[d.status]||'Bilinmeyen durum'} · Son çekim: {d.retrievedAt?.slice(0,10)||'—'}</span><p><a href={d.url} target="_blank" rel="noreferrer">{d.source} kaynağı ↗</a></p></div></article>)}</div><section className="calc-section"><h2>Veri kapsamı</h2><ul><li><a href="https://www.tcmb.gov.tr/kurlar/today.xml">TCMB günlük döviz kurları</a>: anahtarsız XML, iş günlerinde zamanlanmış aktarım. Gösterge niteliğindedir; banka işlem fiyatı değildir.</li><li><a href="https://evds3.tcmb.gov.tr/">TCMB EVDS</a>: TÜFE ve Konut Fiyat Endeksi için hesap anahtarı ve doğrulanmış seri kodu gerekir. Mevduat serisi banka teklifi değildir. Seri tanımı ve frekansı kontrol edilmeden etkinleştirilmez.</li><li><a href="https://data.tuik.gov.tr/">TÜİK</a>: il bazında satış istatistikleri için resmî portal. Bu sürümde otomatik satış tablosu aktarımı yoktur.</li><li><a href="https://www.gib.gov.tr/">GİB</a>: stopaj ve kira vergisi mevzuatının kaynağı. Bu sürümde otomatik vergi oranı servisi yoktur; kullanıcı kendi oranını girer.</li><li><a href="https://parselsorgu.tkgm.gov.tr/">TKGM</a> ve <a href="https://tdth.afad.gov.tr/">AFAD</a>: resmî sorgulama ekranları; tek binanın değer veya dayanıklılık API’si olarak kullanılmaz.</li></ul><h2>Hesaplama yöntemi</h2><p>Net yıllık kira = aylık kira × (12 − boş ay) − işletme gideri − kira gelir vergisi. Reel getiri = (dönem sonu toplamı / başlangıç yatırımı) / (1 + enflasyon) − 1. Mevduat stopajı anaparadan değil, brüt faizden kesilir.</p><p>Yıllık endeks değişiminde tam 12 ay önceki gözlem bulunmazsa oran hesaplanmaz. Eski veri tarih belirtilerek gösterilir; bağlantı hatası örnek sayılarla gizlenmez.</p></section></main><Footer/></>}
