self.addEventListener('push',event=>{let data={};try{data=event.data?.json()||{}}catch{data={body:event.data?.text()}}event.waitUntil(self.registration.showNotification(data.title||'KonutSeyir',{body:data.body||'Yeni emlak haberi yayımlandı.',icon:'/konutseyir-icon.svg',badge:'/konutseyir-icon.svg',tag:data.tag||'konutseyir-news',data:{url:data.url||'/haberler'}}))});
self.addEventListener('notificationclick',event=>{event.notification.close();const target=new URL(event.notification.data?.url||'/haberler',self.location.origin).href;event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const client of list)if(client.url===target&&'focus'in client)return client.focus();return clients.openWindow(target)}))});
